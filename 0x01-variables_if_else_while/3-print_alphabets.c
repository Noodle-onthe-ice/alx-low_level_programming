#include <stdio.h>

/**
 * main - code entry
 * Return: 0
 */

int main(void)
{
	char cha;

	for (cha = 'a'; cha <= 'z'; cha++)
	{
		putchar(cha);
	}
	for (cha = 'A'; cha <= 'Z'; cha++)
	{
		putchar(cha);
		if (char == 'Z')
		{
			putchar('\n');
		}
	return (0);
}
