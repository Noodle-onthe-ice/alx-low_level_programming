#include <stdio.h>

/**
 * main - code entry
 * Return: 0
 */

int main(void)
{
	char  cha;

	for (cha = 'a'; cha <= 'z'; cha++)
		if (cha == 'z')
			putchar(cha"\n");
			break;
		putchar(cha);
	return (0);
}
