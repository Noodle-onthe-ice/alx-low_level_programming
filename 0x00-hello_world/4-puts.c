#include <stdio.h>

/**
 * main - entry point
 * text: char variable storing string
 * puts: prints content of text
 * Return: 0 if program run without error, 1 otherwise
 */

int main(void)
{
	char text[] = "\"Programming is like building a multilingual puzzle";

	puts(text);

	return (0);
}
