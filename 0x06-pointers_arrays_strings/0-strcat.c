#include <stdio.h>
#include <string.h>

/**
 * _strcat - concatenates two strings
 * @dest: parameter
 * @src: parameter
 * Return: *dest
 */

char *_strcat(char *dest, char *src)
{
	_putchar(strcat(*dest, *src))
	putchar('\n')
	return (*dest);
}
